---
import type { Lang } from "@i18n/index";

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  author: string;
  slug: string;
  image?: string;
  lang: Lang;
}
const { title, description, pubDate, author, slug, image, lang } = Astro.props;
const dateStr = pubDate.toLocaleDateString(lang === "sv" ? "sv-SE" : "en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
});
const href = lang === "en" ? `/en/news/${slug}` : `/nyheter/${slug}`;
---

<article class="group bg-[#141414] border border-[#2a2a2a] rounded-xl overflow-hidden hover:border-zinc-600 transition-all duration-300">
  {image && (
    <a href={href} class="block aspect-video overflow-hidden">
      <img
        src={image}
        alt={title}
        loading="lazy"
        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
      />
    </a>
  )}
  <div class="p-6">
    <div class="flex items-center gap-2 text-xs text-zinc-600 mb-3">
      <time datetime={pubDate.toISOString()}>{dateStr}</time>
      <span>&middot;</span>
      <span>{author}</span>
    </div>
    <h3 class="font-bold text-white text-lg leading-snug mb-2 group-hover:text-orange-400 transition-colors">
      <a href={href}>{title}</a>
    </h3>
    <p class="text-zinc-400 text-sm leading-relaxed line-clamp-3 mb-4">
      {description}
    </p>
    <a
      href={href}
      class="text-xs font-semibold text-orange-500 hover:text-orange-400 transition-colors flex items-center gap-1"
    >
      {lang === "sv" ? "Läs mer" : "Read more"}
      <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
        <path d="M5 12h14M12 5l7 7-7 7"/>
      </svg>
    </a>
  </div>
</article>
